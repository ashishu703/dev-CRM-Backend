// Company-related constants used to be defined here (e.g. specific
// organisation names). Those values are now fully dynamic and come
// from the `organizations` table via the API.
//
// This module is kept as a placeholder in case we need non-business
// critical constants in future, but currently exports nothing.

module.exports = {};
